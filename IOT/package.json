{
  
  "name": "traffic-simulation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "docker:up": "docker-compose up -d",
  "docker:down": "docker-compose down",
  "sim": "node simulate_1_crossroad.js",
  "start": "npm run docker:up && npm run sim"
  },
  "dependencies": {
    "mqtt": "^5.10.1",
    "@influxdata/influxdb-client": "^1.35.0"
  }
}